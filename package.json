{
  "name": "ts-import-omit-bug",
  "version": "1.0.0",
  "devDependencies": {
    "typescript": "4.9.3"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "tsc --build && echo \"\\nexport let a = 1\" >> ./src/bug.js && tsc --build"
  }
}
